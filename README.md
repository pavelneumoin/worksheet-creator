# AI Помощник Учителя 🎓

Веб-приложение для автоматической генерации рабочих листов (Worksheets) из фотографий задач. Использует **Yandex Vision** для распознавания текста (OCR) и **YandexGPT** для форматирования задач в LaTeX.

## ✨ Возможности
*   📸 **OCR**: Распознавание текста с изображений задач.
*   🤖 **AI Генерация**: Преобразование текста в красивый LaTeX формат.
*   📄 **PDF Экспорт**: Автоматическая компиляция готового к печати PDF.
*   🎨 **Современный UI**: Удобный и красивый интерфейс.

## 🛠 Технологии
*   **Frontend**: HTML5, CSS3 (Glassmorphism), JavaScript (Vanilla).
*   **Backend**: Python (Flask).
*   **AI/Cloud**: Yandex Cloud (Vision API, Foundation Models).
*   **Typesetting**: LaTeX (pdfLaTeX).

## 🚀 Установка и Запуск

### Предварительные требования
*   Python 3.8+
*   MiKTeX или TeX Live (должен быть в PATH).
*   Аккаунт Yandex Cloud.

### 1. Клонирование и зависимости
```bash
git clone <ваш-репозиторий>
cd teacher-assistant
pip install -r requirements.txt
```

*(Если файла requirements.txt нет, установите вручную)*:
```bash
pip install flask requests
```

### 2. Настройка
Переименуйте файл `backend/config.py.example` в `backend/config.py` и заполните ваши ключи:

```python
FOLDER_ID = 'ваши_id_каталога'
API_KEY = 'ваш_api_ключ'
```

*Важно: Для корректной работы сервисному аккаунту нужны роли `vision.user` и `ai.languageModels.user`.*

### 3. Запуск
```bash
python backend/app.py
```
Открыть в браузере: `http://127.0.0.1:3000`

## 📝 Шаблоны
Вы можете изменить внешний вид рабочего листа, отредактировав файл:
`backend/templates/default_worksheet.tex`
Используйте команды `\TaskBox{№}{Текст}` и `\WriteField{Высота}` для настройки вывода GPT.

## 📄 Лицензия
MIT
