# AI Помощник Учителя 🎓

Веб-приложение для автоматической генерации рабочих листов (Worksheets) из фотографий задач. Использует **GigaChat** (Multimodal) для распознавания текста и задач, а также **GigaChat-Max** для их форматирования в LaTeX и создания новых вариантов.

## ✨ Возможности
*   📸 **Мультимодальное распознавание**: Прямая обработка изображений задач через GigaChat.
*   🤖 **AI Генерация**: Преобразование текста в профессиональный LaTeX формат.
*   🔄 **Генерация похожих**: Создание второго варианта работы с аналогичными задачами (другие числа, та же логика).
*   📄 **PDF Экспорт**: Автоматическая компиляция готового к печати PDF с ответами.
*   🎨 **Современный UI**: Удобный и красивый интерфейс.

## 🛠 Технологии
*   **Frontend**: HTML5, CSS3 (Glassmorphism), JavaScript (Vanilla).
*   **Backend**: Python (Flask).
*   **AI**: Sber GigaChat (Multimodal, Max).
*   **Typesetting**: LaTeX (pdfLaTeX).

## 🚀 Установка и Запуск

### Предварительные требования
*   Python 3.8+
*   MiKTeX или TeX Live (должен быть в PATH).
*   Аккаунт разработчика GigaChat (Sber).

### 1. Клонирование и зависимости
```bash
git clone <ваш-репозиторий>
cd teacher-assistant
pip install -r requirements.txt
```

*(Если файла requirements.txt нет, установите вручную)*:
```bash
pip install flask flask-cors requests gigachat
```

### 2. Настройка
Переименуйте файл `backend/config.py.example` в `backend/config.py` и заполните ваши ключи:

```python
GIGACHAT_CREDENTIALS = 'YOUR_GIGACHAT_AUTHORIZATION_KEY'
```

*Получить ключ можно на [developers.sber.ru](https://developers.sber.ru/).*

### 3. Запуск
```bash
python backend/app.py
```
Открыть в браузере: `http://127.0.0.1:3000`

## 📝 Шаблоны
Вы можете изменить внешний вид рабочего листа, отредактировав файл:
`backend/templates/default_worksheet.tex`
Используйте команды `\TaskBox{№}{Текст}` и `\WriteField{Высота}` для настройки вывода GPT.

## 📄 Лицензия
MIT
